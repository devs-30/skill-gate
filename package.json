{
  "name": "skill-gate",
  "version": "0.2.2",
  "private": true,
  "description": "Gate selected skills behind a confirmation prompt. Tooling-only package manifest (the plugin itself ships no runtime deps).",
  "license": "MIT",
  "author": {
    "name": "Pawel Twardziak @ Devs 3.0",
    "email": "freetools@devs30.com"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run format:check",
    "fix:all": "npm run lint:fix && npm run format",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.0.0",
    "prettier": "^3.0.0"
  }
}
